//! sonic-graph library: shared audio loading, STFT, and 4K rendering
//! used by both the CLI (`sonic-graph`) and the web server
//! (`sonic-graph-web`).

pub mod audio;
pub mod math;
pub mod render;
